version: '3.8'

services:
  zaplink-node:
    build: .
    container_name: zaplink-node
    restart: unless-stopped
    ports:
      - "3001:3001"
    env_file:
      - .env
    environment:
      - NODE_ENV=production
    volumes:
      - ./sessoes:/app/sessoes
      - ./logs:/app/logs
      - ./contatos:/app/contatos
      - ./midia:/app/midia
    # Garante que o Chrome tenha memória compartilhada suficiente
    shm_size: '2gb'
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:3001/api/health", "||", "exit", "1"]
      interval: 30s
      timeout: 10s
      retries: 3
    mem_limit: 3g
